1o Commit: (Payment screen)

class: PaymentScreen extends Screen {
  attributes:

  methods:

  + public PaymentScreen()
  + private void renderMenu()
  + private void handlePaymentSelection(String method)
}



2o Commit: (Search screens)

class: SearchScreen extends Screen {
  attributes:
  + protected TextField searchField;
  + protected Button searchButton;
  + protected ListView<String> resultsList;

  methods:

  + public SearchScreen(String title, int widthOfScreen, int heightOfScreen)
  + private void addResultsList()
  + protected void setupSearchUI()
  + protected void performSearch(String query)
}



3o Commit: (Search screens)

class: SearchHistoryScreen extends SearchScreen {
  attributes:

  methods:

  + public SearchHistoryScreen()
  + protected void performSearch(String query)
}


class: SearchPackageScreen extends SearchScreen {
  attributes:

  methods:

  + public SearchPackageScreen()
  + protected void performSearch(String query)
}



4o Commit: (Confirm screens)

class: class ConfirmScreen extends Screen {
  attributes:

  + protected Text messageText;
  + protected Button confirmButton;
  + protected Button cancelButton;

  methods:

  + public ConfirmScreen(String title, String message)
  + private void renderConfirmationMessage(String message)
  + private void renderButtons()
}


class: class PackageConfirmScreen extends ConfirmScreen {
  attributes:

  methods:

  + public PackageConfirmScreen()


class: class RemoveCooperationConfirmScreen extends ConfirmScreen {
  attributes:

  methods:

  + public RemoveCooperationConfirmScreen()
}
